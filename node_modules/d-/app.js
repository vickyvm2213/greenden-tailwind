var r = require('sync-request');
var fs = require('fs');
var path = require('path');
var wrench = require('wrench');

var basedir = '/home/teu/work/x-reserve',
  names = fs.readFileSync(basedir);

names.forEach(name => {
  var filename =  path.resolve(basedir, name),
    pk = require(path.resolve(basedir, `${package}.json`));
  console.log(name, pk);
  // if (pk.name === name) {
  //   try { wrench.rmdirSyncRecursive(filename); }
  //   catch (err) { };
  // }
});
